<UserControl x:Class="GrandFantasiaINIEditor.Modules.Login.LoginView"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>

        <Grid.Background>
            <ImageBrush ImageSource="/Assets/images/background.png"
Stretch="UniformToFill"/>
        </Grid.Background>

        <Grid Background="#88000000">

            <Border Width="420"
Height="260"
Background="{StaticResource GlassBrush}"
CornerRadius="10"
HorizontalAlignment="Center"
VerticalAlignment="Center"
Padding="25">

                <StackPanel>
                    <TextBlock Text="Grand Fantasia INI Editor"
           FontSize="24"
           HorizontalAlignment="Center"
           Margin="0,0,0,20"
           FontFamily="Segoe UI Semibold">
                        <TextBlock.Foreground>
                            <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                <GradientStop Color="#FFF2A0" Offset="0.0"/>
                                <GradientStop Color="#FFD700" Offset="0.5"/>
                                <GradientStop Color="#B8860B" Offset="1.0"/>
                            </LinearGradientBrush>
                        </TextBlock.Foreground>
                        <TextBlock.Effect>
                            <DropShadowEffect BlurRadius="6" ShadowDepth="2" Opacity="0.4" Color="Black"/>
                        </TextBlock.Effect>
                    </TextBlock>

                    <TextBlock Text="Pasta do Cliente"
Foreground="White"
FontFamily="Segoe UI Semibold"/>

                    <DockPanel Margin="0 8">

                        <TextBox x:Name="ClientPathBox" Height="30" Foreground="White" Background="Transparent" Width="320"/>
                        <Button Content="📁"
Click="Browse_Click" DockPanel.Dock="Right" Width="40" Background="Magenta"/>

                    </DockPanel>

                    <Button Content="Entrar"
Margin="0 20 0 0"
Click="Login_Click"/>

                </StackPanel>

            </Border>

        </Grid>

    </Grid>
</UserControl>