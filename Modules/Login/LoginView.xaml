<UserControl x:Class="GrandFantasiaINIEditor.Modules.Login.LoginView"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
xmlns:loc="clr-namespace:GrandFantasiaINIEditor.Core">

    <Grid>

        <Grid.Background>
            <ImageBrush ImageSource="/Assets/images/background.png"
Stretch="UniformToFill"/>
        </Grid.Background>

        <Grid Background="#88000000">

            <Border Width="420"
Background="{StaticResource GlassBrush}"
CornerRadius="10"
HorizontalAlignment="Center"
VerticalAlignment="Center"
Padding="25">

                <StackPanel>
                    <TextBlock Text="{loc:Loc Login.Title}"
           FontSize="24"
           HorizontalAlignment="Center"
           Margin="0,0,0,20"
           FontFamily="Segoe UI Semibold">
                        <TextBlock.Foreground>
                            <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                <GradientStop Color="#FFF2A0" Offset="0.0"/>
                                <GradientStop Color="#FFD700" Offset="0.5"/>
                                <GradientStop Color="#B8860B" Offset="1.0"/>
                            </LinearGradientBrush>
                        </TextBlock.Foreground>
                        <TextBlock.Effect>
                            <DropShadowEffect BlurRadius="6" ShadowDepth="2" Opacity="0.4" Color="Black"/>
                        </TextBlock.Effect>
                    </TextBlock>

                    <TextBlock Text="{loc:Loc Login.ClientFolder}"
Foreground="White"
FontFamily="Segoe UI Semibold"/>

                    <DockPanel Margin="0 8">

                        <TextBox x:Name="ClientPathBox" Height="30" Foreground="White" Background="Transparent" Width="320" Margin="0,-15,0,15"/>
                        <Button Content="📁" 
Click="Browse_Click" DockPanel.Dock="Right" Width="40" Background="Magenta"/>

                    </DockPanel>

                    <Button Content="{loc:Loc Login.LoginButton}"
Margin="0 20 0 0"
Click="Login_Click"/>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                        <StackPanel.Resources>
                            <Style x:Key="FlagButtonStyle" TargetType="Button">
                                <Setter Property="Margin" Value="12,0"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="Transparent">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </StackPanel.Resources>

                        <Button Click="BtnLang_Click" Tag="pt-BR" Style="{StaticResource FlagButtonStyle}" ToolTip="Português">
                            <Image Source="pack://application:,,,/Assets/images/PT.png" Width="48" Height="32" Stretch="Uniform"/>
                        </Button>
                        <Button Click="BtnLang_Click" Tag="en-US" Style="{StaticResource FlagButtonStyle}" ToolTip="English">
                            <Image Source="pack://application:,,,/Assets/images/EN.png" Width="48" Height="32" Stretch="Uniform"/>
                        </Button>
                        <Button Click="BtnLang_Click" Tag="es-ES" Style="{StaticResource FlagButtonStyle}" ToolTip="Español">
                            <Image Source="pack://application:,,,/Assets/images/ES.png" Width="48" Height="32" Stretch="Uniform"/>
                        </Button>
                        <Button Click="BtnLang_Click" Tag="fr-FR" Style="{StaticResource FlagButtonStyle}" ToolTip="Français">
                            <Image Source="pack://application:,,,/Assets/images/FR.png" Width="48" Height="32" Stretch="Uniform"/>
                        </Button>
                        <Button Click="BtnLang_Click" Tag="zh-CN" Style="{StaticResource FlagButtonStyle}" ToolTip="中文">
                            <Image Source="pack://application:,,,/Assets/images/CH.png" Width="48" Height="32" Stretch="Uniform"/>
                        </Button>
                    </StackPanel>

                </StackPanel>

            </Border>

        </Grid>

    </Grid>
</UserControl>